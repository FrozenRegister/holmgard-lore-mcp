--- a/docs/patch-pipeline-agent-guide.md
+++ b/docs/patch-pipeline-agent-guide.md
@@ -43,3 +43,4 @@ Any path outside the current allowlist — including the excluded agent-instruction/protocol
 documents above — is explicitly out of scope for Phase 1. Track requests to widen it as a
 follow-up issue referencing #554, not as a patch attempt (which will simply be rejected).
+> Smoke test applied.
