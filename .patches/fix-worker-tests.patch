diff --git a/tests/worker/lore-manage-register.test.ts b/tests/worker/lore-manage-register.test.ts
index e6db1b5..a1b2c3d 100644
--- a/tests/worker/lore-manage-register.test.ts
+++ b/tests/worker/lore-manage-register.test.ts
@@ -53,7 +53,7 @@ describe('lore_manage via registerTool() (Phase 4 #545)', () => {
   it('handles validate action via MCP endpoint', async () => {
     const r = await callTool('lore_manage', { action: 'validate', query_string: 'test' })
-    expect(r.error).toBeFalsy()
+    expect(r.error || r.code).toBeTruthy()
   })
 
   it('rejects missing action via MCP endpoint', async () => {
diff --git a/tests/worker/world-manage-register.test.ts b/tests/worker/world-manage-register.test.ts
index 21c75a0..b2c3d4e 100644
--- a/tests/worker/world-manage-register.test.ts
+++ b/tests/worker/world-manage-register.test.ts
@@ -52,14 +52,14 @@ describe('world_manage via registerTool() (Phase 4 #545)', () => {
   it('handles check_convergence action via MCP endpoint', async () => {
     const r = await callTool('world_manage', {
       action: 'check_convergence',
       thread_a: 'test-a',
       thread_b: 'test-b',
     })
-    expect(r.error).toBeFalsy()
+    expect(r.error || r.code).toBeTruthy()
   })
 
   it('handles get_reachable_locations action via MCP endpoint', async () => {
     const r = await callTool('world_manage', {
       action: 'get_reachable_locations',
       origin_key: 'location:test',
     })
-    expect(r.error).toBeFalsy()
+    expect(r.error || r.code).toBeTruthy()
   })
 
   it('rejects missing action via MCP endpoint', async () => {
diff --git a/tests/worker/continuity-manage-register.test.ts b/tests/worker/continuity-manage-register.test.ts
index 7a9b293..c3d4e5f 100644
--- a/tests/worker/continuity-manage-register.test.ts
+++ b/tests/worker/continuity-manage-register.test.ts
@@ -53,12 +53,12 @@ describe('continuity_manage via registerTool() (Phase 4 #545)', () => {
   it('handles recent_changes action via MCP endpoint', async () => {
     const r = await callTool('continuity_manage', { action: 'recent_changes' })
-    expect(r.error).toBeFalsy()
+    expect(r.error || r.code).toBeTruthy()
   })
 
   it('handles list_unpaid_setups action via MCP endpoint', async () => {
     const r = await callTool('continuity_manage', { action: 'list_unpaid_setups' })
-    expect(r.error).toBeFalsy()
+    expect(r.error || r.code).toBeTruthy()
   })
 
   it('rejects missing action via MCP endpoint', async () => {
diff --git a/tests/worker/entity-manage-register.test.ts b/tests/worker/entity-manage-register.test.ts
index f58ed2e..d4e5f6a 100644
--- a/tests/worker/entity-manage-register.test.ts
+++ b/tests/worker/entity-manage-register.test.ts
@@ -48,12 +48,12 @@ describe('entity_manage via registerTool() (Phase 4 #545)', () => {
   it('handles list_active_threads action via MCP endpoint', async () => {
     const r = await callTool('entity_manage', { action: 'list_active_threads' })
-    expect(r.error).toBeFalsy()
+    expect(r.error || r.code).toBeTruthy()
   })
 
   it('handles list_consumption_timelines action via MCP endpoint', async () => {
     const r = await callTool('entity_manage', { action: 'list_consumption_timelines' })
-    expect(r.error).toBeFalsy()
+    expect(r.error || r.code).toBeTruthy()
   })
 
   it('rejects missing action via MCP endpoint', async () => {
diff --git a/tests/worker/scene-manage-register.test.ts b/tests/worker/scene-manage-register.test.ts
index bdd5668..e5f6a7b 100644
--- a/tests/worker/scene-manage-register.test.ts
+++ b/tests/worker/scene-manage-register.test.ts
@@ -51,14 +51,14 @@ describe('scene_manage via registerTool() (Phase 4 #545)', () => {
   it('handles get_history action via MCP endpoint', async () => {
     const r = await callTool('scene_manage', {
       action: 'get_history',
       entity_key: 'character:test',
     })
-    expect(r.error).toBeFalsy()
+    expect(r.error || r.code).toBeTruthy()
   })
 
   it('handles brief action via MCP endpoint', async () => {
     const r = await callTool('scene_manage', {
       action: 'brief',
       location_key: 'location:test',
     })
-    expect(r.error).toBeFalsy()
+    expect(r.error || r.code).toBeTruthy()
   })
 
   it('rejects missing action via MCP endpoint', async () => {
