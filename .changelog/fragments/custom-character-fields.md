### Added

- `character_manage(create)` and `batch_manage(batch_create_characters/batch_create_npcs)` now accept every column on the `characters` table (spell slots, legendary actions/resistances, conditions, resistances/vulnerabilities/immunities, currency, resource pools, faction, behavior, origin, room, perception/stealth bonuses, etc.) instead of hardcoding most of them to schema defaults.
