### Added

- Dependabot config (#574) for `pnpm-lock.yaml` (via the `npm` ecosystem, which supports pnpm lockfile v9+) and `.github/workflows/**` action versions, both weekly with minor/patch updates grouped into a single PR to reduce noise. No dependency-update automation existed before this — updates were entirely manual.
