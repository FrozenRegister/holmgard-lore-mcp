# Shell scripts must stay LF-only regardless of core.autocrlf — git hooks
# (scripts/pre-commit) exec these directly, and a CRLF shebang line breaks them.
scripts/*.sh text eol=lf
scripts/pre-commit text eol=lf
